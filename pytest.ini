[pytest]
# Removed 'python_paths = .' which is deprecated and caused a warning.
# Instead, we will use 'tests/conftest.py' to programmatically fix the import path.

# Set the pattern for test file discovery
python_files = test_*.py